//animated  view
// all the other structs are connected and get accessed here

import SwiftUI

struct ContentView: View {
    var body: some View {
        
       TabBarView()
                
        
        
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
